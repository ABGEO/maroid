---
id: ADR-0000
title: The title of the decision
type: adr
status: proposed
created: YYYY-MM-DD
updated: YYYY-MM-DD
decided:
changes: []
supersedes:
superseded_by:
---

# The title of the decision

Copy this file to `specs/adr/<slug>.md`.
Take the next free number from the index in `README.md`.
Put the number in the `id` field. The file name holds the slug only.

List the identifiers of the rules that change in the `changes` field,
for example `[PLG-004, DAT-002]`. Leave it empty when the decision changes no rule.

## Context

Describe the situation. Give the facts. Do not give the solution here.
Name the requirement identifiers or the defect that caused this ADR.

## Decision

Write the decision in one or two sentences. Use the active voice.

## Rationale

Give the reason for the decision. Give the numbers when numbers exist.

## Alternatives

| Alternative | Why we did not select it |
| ----------- | ------------------------ |
|             |                          |

## Consequences

Describe the result of the decision.
Name each rule that changes, each specification to examine, and each migration to write.

- Positive:
- Negative:
- Work that follows:
